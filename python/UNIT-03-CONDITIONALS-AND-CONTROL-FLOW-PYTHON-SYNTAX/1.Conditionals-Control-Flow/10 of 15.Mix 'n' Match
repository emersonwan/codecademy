# Use boolean expressions as appropriate on the lines below!

# Make me false!
bool_one = (2 <= 2) and "Alpha" == "Bravo"  # We did this one for you!

# Make me true!
bool_two = 4 == 2+2 and "Dog" == "Dog" or 10 < 5

# Make me false!
bool_three = 9**2 < 64 or not 25**0.5 == 5

# Make me true!
bool_four = not -(-2) >= 5 and 11 < 12 and "Dog" != "Cat"

# Make me true!
bool_five = 20 ** 2 < 30 ** 2 and not 5 > 6 or 9 < 8
